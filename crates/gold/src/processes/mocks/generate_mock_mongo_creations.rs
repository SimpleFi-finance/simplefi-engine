use bronze::mongo::evm::data_sets::logs::Log;

pub async fn generate_mock_mongo_creations(
    protocol_id: &str,
    year: i32,
) -> Vec<Log> {
    todo!()
}

pub async fn generate_mock_mongo_creations_update(
    factory_addresses: Vec<String>,
    from: i64,
) -> Vec<Log> {
    todo!()
}
