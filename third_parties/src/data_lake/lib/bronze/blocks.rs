// define parquet formats
// pub async fn save_blocks(chain_block_schema: &Schema, blocks: &Vec<Block>, location: &String) -> Result<(), Error> {
//     // load location to save files
//     // create parquet writer
//     // let mut parquet_writer = ParquetWriter::new(chain_driver, "blocks").await?;
//     // for block in blocks {
//     //     parquet_writer.write(block).await?;
//     // }
//     // parquet_writer.close().await?;
//     // Ok(())
// }


